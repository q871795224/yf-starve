# 项目开发约定

## DST 骑乘动画的显示主体

- 未骑乘时，牛的可见动作由牛实体的 `AnimState` 和 `beefalo` 动画银行负责。
- 骑乘后，原版会把骑手切换到 `wilsonbeefalo` 动画银行，并把牛的外观 build 应用到骑手的 `AnimState`；画面上的骑手与牛组合体主要由骑手实体渲染。
- 因此骑乘状态下的可见动作必须优先在骑手的 `wilson` / `wilson_client` 状态图中播放，并使用 `wilsonbeefalo` 银行中存在的动画名，例如 `bellow`。
- 只调用 `beefalo.sg:GoToState(...)` 可以改变牛的逻辑状态、声音或伤害流程，但不能当作骑乘画面已经会改变；要单独确认骑乘组合体的可见动画。

## 相关概念

- **动画银行（bank）**：同一套视觉骨架可使用的动作集合，例如 `wilsonbeefalo`。
- **动画片段（animation）**：银行中的具体动作，例如 `bellow`、`run_loop`。
- **Build**：动作显示所需的图片、符号和材质组合。
- **StateGraph**：决定何时进入状态、播放哪个片段以及何时结算逻辑。

实现或排查骑乘动作时，先确认目标动作属于哪个 bank，再分别检查服务器逻辑状态、客户端状态图和可见 `AnimState`；不要把牛实体的 `beefalo` 动画状态等同于骑乘组合体已经显示。

## 独立验证边界

- `anim.bin` 可以脱离 DST 解析和预览；资源层检查应先确认 bank、动作名、帧数和帧率。
- 一个 zip 往往只是同名 bank 的追加分片。看到 `wilsonbeefalo.zip` 中没有 `bellow`，不能推断原版 bank 没有 `bellow`。
- 预览工具需要匹配的 `build.bin` 和贴图。不要拿其他生物的 build 代替 beefalo build 来判断骑乘组合体是否正确。
- 独立预览只能证明资源帧存在；RPC、StateGraph、挂载主体和联机同步仍要在 DST 中验证。

## 已完成的项目成果

- 已建立 Lua 模组骨架，入口是 `mod/modmain.lua`，配置入口是 `mod/modinfo.lua`；参考模组、官方素材和临时工具隔离在 `temp/`，不直接作为发布包内容。
- 已用 Git 管理项目并推送到 `q871795224/yf-starve`。代码修改完成后先提交，再同步到测试机。
- 已完成几个独立机制原型：人牛同步攻击、骑乘战吼、蓄力冲撞和骑乘犁地。默认快捷键分别沿用现有配置：战吼 H、冲撞 J、犁地 L。它们的伤害、恐惧、寻路和数值平衡仍不能视为最终设计。
- 已搭建离线动画实验室和动画资源检查工具，可以从官方 depot / 参考模组读取 bank、build、贴图并逐帧预览。
- 已完成骑枪刺击的 23 帧自制动画资源：基于官方 Beefalo 攻击动作制作，枪先前挺、再随牛上顶转向、最后收回；资源编译为 `mod/bank/yf_mounted_lance.zip`，枪的 build 放在 `mod/anim/swap_spear_lance.zip`。
- 骑枪 B 键已接入 `wilson` / `wilson_client` 状态图，并与牛的原版 `atk_pre` → `atk` 演出同步。目前只播放视觉，不调用 `combat:DoAttack`，避免和原版牛攻击重复结算伤害。
- 108 测试机的实际模组目录是：`D:\SteamLibrary\steamapps\common\Don't Starve Together\mods\yf-starve-dev`。仓库目录 `/mnt/c/Users/sf/Documents/workspace/yf-starve` 用于同步代码；真正加载游戏的是 Steam 安装目录下的模组目录。

## 已踩过的坑与固定规则

### 骑乘主体与动画 bank

- 骑乘画面主要由骑手实体渲染。把动作只放到牛实体的 `beefalo` 状态图，不能证明骑手和牛的组合体会显示对应动作。
- 自定义骑乘动作必须在骑手的 `wilson` / `wilson_client` 状态图触发，目标 bank 是 `wilsonbeefalo`。
- `wilson_client` 需要注册同名视觉状态，并通过 `server_states` 与服务器状态对应；只注册服务器状态会出现逻辑执行但客户端不播放的情况。

### `bank/` 与 `anim/` 的资源边界

- 只有 `anim.bin` 的动画 bank 追加包必须放在 `bank/`，例如 `bank/yf_mounted_lance.zip`，并用 `Asset("ANIM", "bank/...")` 加载。
- 包含 build、atlas 和贴图的资源放在 `anim/`，例如 `anim/swap_spear_lance.zip`。
- 曾经把只有 `anim.bin` 的骑枪包放到 `anim/`，游戏虽然加载了 mod 和快捷键，但播放动作时出现 `FROMNUM`，进入状态后骑乘组合体消失。以后新增 bank 时先按这个边界检查路径和 zip 内容。

### 队列动画与 `animover`

- `PlayAnimation(pre)` 后 `PushAnimation(attack, false)` 时，`animover` 可能在 `pre` 结束、切换到队列中的下一段时就触发。
- 状态不能在每次 `animover` 都直接回到 `idle`。结束处理必须先检查 `inst.AnimState:AnimDone()`，确认整个队列完成后再退出；已有骑枪状态按这个规则处理。

### 武器 build、图层和手部

- 不要在通用骑乘攻击状态里无条件 `OverrideSymbol` 成 `swap_spear_lance`。这样会把剑、斧、火把等当前武器都替换成枪；空手时则没有枪的来源。
- 当前骑枪原型把枪直接写进动画帧，只适合专用骑枪演示，不适合作为通用武器攻击。后续要么让动作保留当前的 `swap_object`，要么明确要求装备专用骑枪后才触发。
- 不能通过复制手部图层解决遮挡；之前已经出现过多出两只手的问题。需要移动已有手和手臂的图层，或单独调整前景层。
- Beefalo 的头部图层会遮住骑手手臂。骑手攻击辨识度要优先调整已有手、前臂和武器的 z 顺序，避免把整个人物粗暴地覆盖到牛身前造成穿模。
- 离线预览可以把官方 build 合并成一个完整场景，但游戏内使用的是骑手当前 build；如果动画引用独立 build，必须通过 `OverrideSymbol` 明确映射，并在状态结束时清理临时覆盖。

### 动画名称与方向

- DST 状态图通常传不带方向后缀的基础动作名，例如 `run_pre`、`player_atk`。自定义资源为了让游戏端解析稳定，骑枪包内使用 `yf_mounted_lancejab_pre` 和 `yf_mounted_lancejab` 这类基础名称；浏览器预览仍可保留 `_side` 版本。
- 方向后缀 `_side`、`_upside`、`_downside` 代表不同 facing 变体。只有 `_side` 的动作不能假定上下方向也自然；正式使用前需要补齐方向或明确限制朝向。

## 当前已知问题

- 骑枪动作已经能在测试机加载并完成整段播放，但起始姿势还没有完全接上原版骑乘 idle：当前状态直接进入自定义准备动作。
- 骑枪动画仍然是专用枪视觉，尚未保留玩家手里的任意武器；B 键目前也没有真正的骑枪物品或装备条件。
- 牛的头部和身体动作幅度较大，骑手的手臂与武器容易被遮住。当前只确认了资源和基本节奏，尚未确定最终的前景层、攻击特效或镜头表现。
- 骑枪目前不结算伤害，不能把 B 键的动作当作已经完成的战斗技能。
- 108 上的游戏内验证需要重启游戏或重新加载模组才能读取新的 Lua、bank 和 build；仅替换文件不会刷新已经加载的资源。

## 后续工作顺序

1. 让骑枪动作从原版 `idle_loop` 的真实持武器姿势开始，保留一只手持武器、另一只手搭在牛身上的状态。
2. 决定 B 键是“通用骑乘攻击”还是“专用骑枪攻击”。通用攻击保留 `swap_object`；专用骑枪则新增物品/装备条件，不能让任意武器自动变成枪。
3. 调整已有手、前臂和武器的前景图层，让骑手攻击可见，同时检查是否穿过牛头、牛角或鞍具；不要复制手部图层。
4. 根据测试结果增加枪尖轨迹、命中特效或更大的专用骑枪素材，提升攻击辨识度；先完成图层和姿势，再决定是否需要额外特效。
5. 补齐方向动作或建立明确的侧向限制，再接入真实命中帧、武器伤害和牛的攻击配合，避免产生重复伤害。
6. 在 108 上逐项回归：普通武器、空手、专用骑枪、上下左右朝向、连续按键、下牛/受击/死亡、多人客户端可见性，并记录 `client_log.txt` 中的 mod 日志和资源错误。
7. 机制稳定后再接技能点、解锁条件、存档持久化和技能树 UI；技能树不应反过来阻碍单个机制的测试。
